"use client";
import Link from "next/link";
import { useState } from "react";
import { createClient } from "../../lib/supabase/client";
export default function ForgotPasswordPage() { const [email,setEmail]=useState(""); const [message,setMessage]=useState(""); const [sending,setSending]=useState(false); async function requestReset(event){event.preventDefault();setSending(true);const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/update-password`});setSending(false);setMessage(error?"We couldn't send a reset email. Try again shortly.":"If that address has an account, a reset link is on its way.");} return <main className="auth-page"><section className="auth-card"><img className="portal-logo" src="/ot.webp" alt="Theta Tau" /><p className="kicker">ACCOUNT RECOVERY</p><h1>Reset password</h1><p className="lead">Enter your invited pledge-class email and we’ll send a secure reset link.</p><form onSubmit={requestReset}><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} placeholder="name@example.com" required/><button className="primary" disabled={sending} type="submit">{sending?"Sending…":"Send reset link"}</button></form><p className="notice">{message}</p><Link className="text-link" href="/">Back to sign in</Link></section></main>; }
